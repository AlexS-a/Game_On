<div class="booking-results">
  <h1 class="text-center">Here are the booking details for <%= @booking.game.name %></h1>
    <div class="booking-card" style="background-image: linear-gradient(-225deg, rgba(0,101,168,0.6) 0%, rgba(0,36,61,0.6) 50%), url(<%= image_path("GTA.jpg", width: 800, height: 500, crop: :thumb) %>)">
      <div class="booking-description">
        <h1><%= @booking.rent_start %></h1>
        <h1><%= @booking.rent_end %></h1>
        <h1>For an an amount of £<%= @booking.game.price %> per week</h1>
        <%= link_to @booking.game.user.user_name, user_path(@booking.game.user) %>
        <%= @booking.status %>
      </div>
    </div>
</div>
